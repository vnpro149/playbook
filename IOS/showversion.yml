---
- name: Show version
  hosts: all
  tasks:
    - name: checkversion
      ios_command:
        commands: 
          - show version
        wait_for: 
          - result[0] contains IOS
      register: result[0]
    - debug: var=result.stdout_lines
