---
- name: check version
  hosts: all
  version: "15.5(4)M"
  tasks:
    - name: checkversion
      ios_command:
        commands: 
          - show version
      register: show_ver
    - fail: msg="Wrong Cisco IOS version"
      when: "not ('Version {{version}}' in show_ver.stdout[0])"
